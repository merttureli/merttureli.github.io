/**
 * The only file to edit after signing up for things.
 *
 * Both values are empty by default and every consumer no-ops when empty, so
 * the site is always safe to push in this state.
 */
window.MT_ANALYTICS = {
  // Deployed Cloudflare Worker collect endpoint, ending in /c
  // e.g. "https://portfolio-tracker.merttureli.workers.dev/c"
  endpoint: "https://portfolio-tracker.merttureli.workers.dev/c",

  // Microsoft Clarity project id from clarity.microsoft.com
  // e.g. "abc123xyz"
  clarity: "xzb4tyylxd",
};
